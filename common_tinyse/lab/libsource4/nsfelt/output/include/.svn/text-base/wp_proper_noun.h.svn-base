/*
 * 挑选专有名模块头文件
 */

#include <ul_dictmatch.h>

bool isProperNoun(dm_dict_t* pDmDict,
		dm_pack_t* pDmPack, const char* query, int queryLength);
bool pickProperNoun(dm_dict_t* pDmDict, const scw_out_t* pOut, 
		dm_pack_t* pDmPack, const char* query, int queryLength, 
		char inProperNoun[]);
